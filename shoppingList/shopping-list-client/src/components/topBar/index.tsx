import Typography from "@mui/material/Typography";

export default function TopBar() {
    return (
        <Typography align="center" sx={{
            fontSize: 30,
            fontWeight: "Bold",
            height: "10vh"
        }}>רשימת קניות </Typography>
    );
}